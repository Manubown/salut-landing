/**
 * The live Salut web app (an installable PWA). The landing funnels people here —
 * "get the app" CTAs and per-event deep links (`${APP_URL}/events/:id`). Single
 * place to change if the app domain moves.
 */
export const APP_URL = 'https://salut-web.bressler.at';
